<UserControl x:Class="Labs.RemotePlay.Panels.ScriptsBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgSubtle}">
    <Border BorderBrush="{StaticResource BorderSoft}" BorderThickness="0,0,0,1" Padding="14,8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="script" FontFamily="{StaticResource FontMono}" FontSize="11"
                       Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox Grid.Column="1" Name="ScriptCombo" MinWidth="220"
                      VerticalAlignment="Center"/>
            <Button Grid.Column="2" Content="reload" Style="{StaticResource BtnQuiet}" Margin="8,0"
                    Click="Reload_Click"/>
            <Button Grid.Column="3" Name="RunBtn"  Content="run"  Style="{StaticResource BtnPrimary}"
                    Margin="8,0,4,0" Click="Run_Click"/>
            <Button Grid.Column="4" Name="StopBtn" Content="stop" Style="{StaticResource BtnGhost}"
                    IsEnabled="False" Click="Stop_Click"/>
            <TextBlock Grid.Column="1" Name="StatusText" HorizontalAlignment="Right" VerticalAlignment="Center"
                       FontFamily="{StaticResource FontMono}" FontSize="11"
                       Foreground="{StaticResource TextDim}"/>
        </Grid>
    </Border>
</UserControl>
