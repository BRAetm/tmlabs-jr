<Window x:Class="Labs.RemotePlay.PsnLoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="sign in with psn" Height="760" Width="560"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="{DynamicResource Bg}" Foreground="{DynamicResource Text}"
        FontFamily="Segoe UI" FontSize="13">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" ResizeBorderThickness="6"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Branded caption -->
        <Grid Grid.Row="0" Background="{DynamicResource BgSubtle}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0">
                <Canvas Width="18" Height="18" VerticalAlignment="Center">
                    <Path Fill="{DynamicResource Accent}"
                          Data="M 2,2 L 8,2 L 8,12 L 16,12 L 16,16 L 2,16 Z"/>
                    <Ellipse Canvas.Left="11" Canvas.Top="3" Width="3" Height="3" Fill="{StaticResource Blue400}"/>
                </Canvas>
                <TextBlock Text="labs engine" Margin="10,0,0,0" VerticalAlignment="Center"
                           FontFamily="Segoe UI" FontSize="12"
                           FontWeight="SemiBold" Foreground="{DynamicResource Text}"/>
                <TextBlock Text="/ sign in with psn" Margin="8,0,0,0" VerticalAlignment="Center"
                           FontFamily="Cascadia Mono, Consolas" FontSize="10"
                           Foreground="{StaticResource TextMuted}"/>
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource CaptionCloseBtn}"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Click="Cancel_Click"/>
        </Grid>

        <Border Grid.Row="1" Background="{DynamicResource Surface}" Padding="16,10">
            <TextBlock Name="StatusText" Text="loading PSN sign-in…" Foreground="{StaticResource TextMuted}"/>
        </Border>

        <wv2:WebView2 Grid.Row="2" Name="Web"/>

        <Border Grid.Row="3" Background="{DynamicResource Surface}" Padding="16,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="cancel" Click="Cancel_Click" Padding="16,8"
                        Background="Transparent" Foreground="{DynamicResource Text}" BorderThickness="0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
