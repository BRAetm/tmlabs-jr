<Window x:Class="Labs.RemotePlay.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="settings" Height="460" Width="440"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None"
        Background="{DynamicResource Bg}" Foreground="{DynamicResource Text}"
        FontFamily="Segoe UI" FontSize="14">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" ResizeBorderThickness="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <DockPanel LastChildFill="True">
        <Grid DockPanel.Dock="Top" Height="40" Background="{DynamicResource BgSubtle}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0">
                <Canvas Width="18" Height="18" VerticalAlignment="Center">
                    <Path Fill="{DynamicResource Accent}"
                          Data="M 2,2 L 8,2 L 8,12 L 16,12 L 16,16 L 2,16 Z"/>
                    <Ellipse Canvas.Left="11" Canvas.Top="3" Width="3" Height="3" Fill="{StaticResource Blue400}"/>
                </Canvas>
                <TextBlock Text="labs engine" Margin="10,0,0,0" VerticalAlignment="Center"
                           FontFamily="{StaticResource FontDisplay}" FontSize="12"
                           FontWeight="SemiBold" Foreground="{DynamicResource Text}"/>
                <TextBlock Text="/ settings" Margin="8,0,0,0" VerticalAlignment="Center"
                           FontFamily="{StaticResource FontMono}" FontSize="10"
                           Foreground="{StaticResource TextMuted}"/>
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource CaptionCloseBtn}"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Click="Close_Click"/>
        </Grid>
    <StackPanel Margin="24">
        <TextBlock Text="Stream settings" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <TextBlock Text="Resolution" Margin="0,8,0,4"/>
        <ComboBox Name="ResolutionBox" Background="{DynamicResource Surface}" Foreground="{DynamicResource Text}" Padding="8" BorderThickness="0">
            <ComboBoxItem Content="720p"/>
            <ComboBoxItem Content="1080p" IsSelected="True"/>
        </ComboBox>

        <TextBlock Text="Frame rate" Margin="0,16,0,4"/>
        <ComboBox Name="FpsBox" Background="{DynamicResource Surface}" Foreground="{DynamicResource Text}" Padding="8" BorderThickness="0">
            <ComboBoxItem Content="30"/>
            <ComboBoxItem Content="60" IsSelected="True"/>
        </ComboBox>

        <TextBlock Text="Codec" Margin="0,16,0,4"/>
        <ComboBox Name="CodecBox" Background="{DynamicResource Surface}" Foreground="{DynamicResource Text}" Padding="8" BorderThickness="0">
            <ComboBoxItem Content="H264"/>
            <ComboBoxItem Content="H265" IsSelected="True"/>
        </ComboBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,32,0,0">
            <Button Content="Close" Click="Close_Click" Padding="16,8"
                    Background="{DynamicResource Accent}" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </StackPanel>
    </DockPanel>
</Window>
