<UserControl x:Class="Labs.RemotePlay.Panels.XboxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Background="Black">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Grid.RowSpan="2">
        <wv2:WebView2 Name="Web"/>
        <Border Background="#80000000" VerticalAlignment="Top" Padding="12,6">
            <DockPanel LastChildFill="False">
                <TextBlock Name="Hud" Foreground="#B0FFFFFF" FontSize="11"
                           FontFamily="Cascadia Mono, Consolas" VerticalAlignment="Center"/>
                <Button Content="stop" DockPanel.Dock="Right"
                        Background="Transparent" Foreground="White" BorderThickness="0"
                        Padding="10,4" Click="Stop_Click"/>
            </DockPanel>
        </Border>
        </Grid>
    </Grid>
</UserControl>
