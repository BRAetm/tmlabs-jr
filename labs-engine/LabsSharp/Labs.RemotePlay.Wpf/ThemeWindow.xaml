<Window x:Class="Labs.RemotePlay.ThemeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:local="clr-namespace:Labs.RemotePlay"
        Title="theme" Height="660" Width="520"
        Background="{DynamicResource Bg}" Foreground="{DynamicResource Text}"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" UseLayoutRounding="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="44" ResizeBorderThickness="4"
                            CornerRadius="0" GlassFrameThickness="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="QuickSwatch" TargetType="Button">
            <Setter Property="Width" Value="22"/>
            <Setter Property="Height" Value="22"/>
            <Setter Property="Margin" Value="0,0,6,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2E3F6A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="2" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Border.Background>
                                <SolidColorBrush Color="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Tag, Converter={x:Static local:HexToColorConverter.Instance}}"/>
                            </Border.Background>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Caption bar -->
        <Grid Grid.Row="0" Background="{DynamicResource BgSubtle}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="theme" VerticalAlignment="Center" Margin="18,0,0,0"
                       FontFamily="{StaticResource FontDisplay}" FontSize="14" FontWeight="SemiBold"/>
            <Button Grid.Column="1" Content="✕" Width="44" Height="44"
                    Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextMuted}"
                    FontFamily="{StaticResource FontMono}" Cursor="Hand"
                    shell:WindowChrome.IsHitTestVisibleInChrome="True"
                    Click="Close_Click"/>
        </Grid>

        <!-- Body -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="28,24,28,24">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="THEME" Style="{StaticResource Eyebrow}"/>
                        <TextBlock x:Name="ThemeHint" Text="pick a theme · accent stays editable"
                                   Style="{StaticResource Caption}" Margin="0,4,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="1" x:Name="RemoveThemeBtn" Content="remove theme"
                            Style="{StaticResource BtnQuiet}" Click="RemoveTheme_Click"
                            VerticalAlignment="Bottom"/>
                </Grid>

                <!-- Preset tiles — color presets + custom image slot, all equal weight -->
                <UniformGrid x:Name="PresetsRow" Columns="5" Margin="0,14,0,0" Rows="1"/>

                <!-- Image stretch / opacity — only meaningful while 'image' preset is active -->
                <Grid x:Name="ImageControls" Margin="0,14,0,0" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="STRETCH" Style="{StaticResource Eyebrow}"/>
                        <ComboBox x:Name="StretchCombo" Width="150" Margin="0,6,0,0"
                                  SelectionChanged="Stretch_Changed">
                            <ComboBoxItem Content="UniformToFill"/>
                            <ComboBoxItem Content="Uniform"/>
                            <ComboBoxItem Content="Fill"/>
                            <ComboBoxItem Content="None"/>
                        </ComboBox>
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="OPACITY" Style="{StaticResource Eyebrow}"/>
                        <Slider x:Name="OpacitySlider" Width="140" Margin="0,10,0,0"
                                Minimum="0.05" Maximum="1" Value="1" SmallChange="0.05"
                                ValueChanged="Opacity_Changed"/>
                    </StackPanel>
                    <Button Grid.Column="4" Content="change image" Style="{StaticResource BtnGhost}"
                            VerticalAlignment="Bottom" Click="ChooseImage_Click"/>
                </Grid>

                <TextBlock Text="COLORS" Style="{StaticResource Eyebrow}" Margin="0,30,0,4"/>
                <TextBlock x:Name="ColorsHint" Text="accent only · remove theme to unlock all colors"
                           Style="{StaticResource Caption}" Margin="0,0,0,14"/>

                <StackPanel x:Name="ColorRows"/>

                <!-- Hidden: preserves existing code-behind references without UI -->
                <TextBox x:Name="ImagePathBox" Visibility="Collapsed" IsReadOnly="True"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="{DynamicResource BgSubtle}" Padding="18,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="reset" Click="Reset_Click" Style="{StaticResource BtnGhost}"/>
                <Button Grid.Column="2" Content="save" Click="Save_Click" Style="{StaticResource BtnPrimary}"/>
            </Grid>
        </Border>

        <!-- Shared color picker popup — repositioned per swatch click -->
        <Popup x:Name="ColorPop" StaysOpen="False" AllowsTransparency="True"
               Placement="Right" HorizontalOffset="8" PopupAnimation="Fade">
            <Border Background="{DynamicResource BgSubtle}" BorderBrush="#2E3F6A" BorderThickness="1"
                    CornerRadius="3" Padding="14">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="220"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="26"/>
                        </Grid.ColumnDefinitions>

                        <!-- SV region (saturation × value) -->
                        <Border Grid.Column="0" Width="220" Height="160" ClipToBounds="True"
                                MouseLeftButtonDown="Sv_Down" MouseMove="Sv_Move" MouseLeftButtonUp="Sv_Up">
                            <Grid>
                                <Rectangle>
                                    <Rectangle.Fill>
                                        <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                            <GradientStop Color="#FFFFFFFF" Offset="0"/>
                                            <GradientStop x:Name="HueStop" Color="#FFFF0000" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Rectangle.Fill>
                                </Rectangle>
                                <Rectangle>
                                    <Rectangle.Fill>
                                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                            <GradientStop Color="#00000000" Offset="0"/>
                                            <GradientStop Color="#FF000000" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Rectangle.Fill>
                                </Rectangle>
                                <Canvas IsHitTestVisible="False">
                                    <Ellipse x:Name="SvThumb" Width="14" Height="14"
                                             Stroke="White" StrokeThickness="2" Fill="Transparent"/>
                                </Canvas>
                            </Grid>
                        </Border>

                        <!-- Hue slider -->
                        <Border Grid.Column="2" Width="26" Height="160" ClipToBounds="True"
                                MouseLeftButtonDown="Hue_Down" MouseMove="Hue_Move" MouseLeftButtonUp="Hue_Up">
                            <Grid>
                                <Rectangle>
                                    <Rectangle.Fill>
                                        <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                            <GradientStop Color="#FF0000" Offset="0"/>
                                            <GradientStop Color="#FFFF00" Offset="0.17"/>
                                            <GradientStop Color="#00FF00" Offset="0.33"/>
                                            <GradientStop Color="#00FFFF" Offset="0.5"/>
                                            <GradientStop Color="#0000FF" Offset="0.67"/>
                                            <GradientStop Color="#FF00FF" Offset="0.83"/>
                                            <GradientStop Color="#FF0000" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Rectangle.Fill>
                                </Rectangle>
                                <Canvas IsHitTestVisible="False">
                                    <Rectangle x:Name="HueThumb" Width="26" Height="3"
                                               Fill="White" Opacity="0.95"/>
                                </Canvas>
                            </Grid>
                        </Border>
                    </Grid>

                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="36" Height="36" CornerRadius="2"
                                BorderBrush="#2E3F6A" BorderThickness="1">
                            <Border.Background>
                                <SolidColorBrush x:Name="PreviewBrush" Color="#3B82F6"/>
                            </Border.Background>
                        </Border>
                        <TextBox x:Name="HexField" Grid.Column="1"
                                 FontFamily="{StaticResource FontMono}" FontSize="13"
                                 Margin="10,0,0,0" Padding="10,8" VerticalContentAlignment="Center"
                                 TextChanged="HexField_Changed"/>
                    </Grid>

                    <!-- Quick swatches -->
                    <WrapPanel Margin="0,12,0,0" Orientation="Horizontal">
                        <Button Tag="#3B82F6" Style="{StaticResource QuickSwatch}" Click="Quick_Click"/>
                        <Button Tag="#7C3AED" Style="{StaticResource QuickSwatch}" Click="Quick_Click"/>
                        <Button Tag="#EC4899" Style="{StaticResource QuickSwatch}" Click="Quick_Click"/>
                        <Button Tag="#F59E0B" Style="{StaticResource QuickSwatch}" Click="Quick_Click"/>
                        <Button Tag="#10B981" Style="{StaticResource QuickSwatch}" Click="Quick_Click"/>
                        <Button Tag="#14B8A6" Style="{StaticResource QuickSwatch}" Click="Quick_Click"/>
                        <Button Tag="#EF4444" Style="{StaticResource QuickSwatch}" Click="Quick_Click"/>
                        <Button Tag="#FFFFFF" Style="{StaticResource QuickSwatch}" Click="Quick_Click"/>
                        <Button Tag="#000000" Style="{StaticResource QuickSwatch}" Click="Quick_Click"/>
                    </WrapPanel>
                </StackPanel>
            </Border>
        </Popup>
    </Grid>

</Window>
